type: object
additionalProperties: false
properties:
  partition:
    type: integer
    minValue: 0
  offset:
    type: integer
    minValue: 0
    # Not true but we need a Long
    format: utc-millisec
  timestamp:
    type: integer
    format: utc-millisec
